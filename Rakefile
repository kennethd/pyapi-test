VERSION_FILE = "./pyapi/VERSION"

APP_PKGNAME = "pyapi-test"
APP_MODNAME = "pyapi"

OPT_DEPS = ["dev", "flask", "mongo", "pdf", "postgres", "redis"]

DOCKER_CONTAINER_NAME = "pyapi"
DOCKER_REPO_NAME = "pyapi-test"

