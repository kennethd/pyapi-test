VERSION_FILE = "./pyapi/VERSION"

APP_PKGNAME = "pyapi-test"
APP_MODNAME = "pyapi"

# not used by this project
OPT_DEPS = []

DOCKER_CONTAINER_NAME = "pyapi"
DOCKER_REPO_NAME = "pyapi-test"

# this is a meta var; allows setting PYAPI_TEST_TAG in your shell before
# building to bypass rakelib auto-tagging conventions, useful when working on
# rakelib itself to avoid breaking things for other devs
DOCKER_ENV_CUSTOM_TAG = "PYAPI_TEST_TAG"
